"use strict"; //NOTE - Treat all JS code as newer version

//NOTE - If we run alert( 3 + 3 ) on terminal we get an error just because, this type of syntax can run on only console and it is not understandable by node. Because, node have different syntax for this.

let name = "Aryan";
let age = 20
